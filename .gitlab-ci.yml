create-pages:
    script:
        - mkdir -p public
            - cp -r index.html assets css js public/
              pages: true
                rules:
                    - if: '$CI_COMMIT_BRANCH == "main"'